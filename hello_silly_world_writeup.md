# Hello Silly World

Type: Unknown

## Summary

This challenge is a welcome prompt rather than a technical exploit. The description explicitly states:

```text
The flag for this challenge is `sillyCTF{welcome_to_silly_ctf_2!}`
```

There were no local challenge files or remote targets needed for extraction. Recon confirmed the workspace did not contain artifacts for this challenge, so the solve was simply to read the provided description carefully.

## Solve

Read the challenge text and extract the flag directly:

```text
sillyCTF{welcome_to_silly_ctf_2!}
```

## Flag

```text
sillyCTF{welcome_to_silly_ctf_2!}
```
